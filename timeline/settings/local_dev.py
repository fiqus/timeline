from .base import *

MIDDLEWARE.append('timeline.middleware.dev_cors_middleware')

SPATIALITE_LIBRARY_PATH = 'mod_spatialite.so'

LOGGING = {
    'disable_existing_loggers': False,
    'version': 1,
    'filters': {
        'require_debug_true': {
            '()': 'django.utils.log.RequireDebugTrue',
        }
    },
    'handlers': {
        'console': {
            'level': 'DEBUG',
            'filters': ['require_debug_true'],
            'class': 'logging.StreamHandler',
        }
    },
    'loggers': {
        'django.db.backends': {
            'level': 'DEBUG',
            'handlers': ['console'],
        }
    }
}